executa meu arquivo java:

```  javac App.java  ```

Ele gera um arquivo .class baseado no arquivo .java executado
Depois do arquivo gerado, basta usarmos o comando:

```  java App  ```

Assim executamos nosso codigo com sucesso!